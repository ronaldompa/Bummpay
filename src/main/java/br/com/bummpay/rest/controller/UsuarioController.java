/*
package br.com.bummpay.rest.controller;

import br.com.bummpay.domain.entity.Usuario;
import br.com.bummpay.service.impl.UsuarioServiceImpl;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.web.bind.annotation.*;

import javax.validation.Valid;

@RestController
@RequestMapping("/api/usuarios")
@RequiredArgsConstructor
public class UsuarioController {

    private final UsuarioServiceImpl usuarioService;

    @PostMapping
    @ResponseStatus(HttpStatus.CREATED)
    public Usuario salver(@Valid @RequestBody Usuario usuario){
        return usuarioService.salvar(usuario);
    }
*/
/*
    @GetMapping
    @ResponseStatus(HttpStatus.CREATED)
    public List<Usuario> listar(@Valid @RequestBody Usuario usuario){
        return usuarioService.salvar(usuario);
    }*//*



}
*/
